# Enrolment portraits pulled off the terminals. These are photographs of real
# people and this repository is on GitHub, so they stay on the server that
# collected them.
data/photos/

# Copies the server makes before rewriting a store it did not recognise.
*.server-backup

# The HR door's access log. Grows per call and is local operational history.
data/hrlog.jsonl

# Webhook targets and their delivery log: local operational state.
data/hooks.json
data/hooklog.jsonl
